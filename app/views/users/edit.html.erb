<% content_for :meta_title, "Edit #{ @user.name }'s details | #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, "Edit the details of #{ @user.name }'s details" %>

<div class="col-sm-6 offset-sm-3 my-5">
  <h2>Edit a current seeker</h2>
  <%= render 'edit_form' %>

  <%= link_to "Go back to all seekers", seekers_path, class: "pt-2" %>
</div>
